# Test data

`input/` contains a test dataset. To check that Classika works as expected, run the test dataset with default settings, but change **Number of data splits to 2**. Compare your output to the contents of the `output/` directory.
